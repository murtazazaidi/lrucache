{
  "name": "simple-lrucache",
  "version": "1.0.1",
  "description": "Least Recently Used Cache based on JS",
  "main": "lib/LRUCache.js",
  "scripts": {
    "build": "babel src -d lib",
    "test": "npm run build && nyc mocha --require babel-register ./test/**/*.spec.js",
    "prepublish": "npm run test",
    "report-coverage": "nyc npm test && nyc report --reporter=text-lcov > coverage.lcov && codecov"
  },
  "keywords": [
    "lru",
    "cache",
    "lrucache",
    "least recently used cache",
    "simple",
    "simple cache",
    "simple-lrucache"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/murtazazaidi/lrucache.git"
  },
  "bugs": "https://github.com/murtazazaidi/lrucache/issues",
  "author": "Murtaza Zaidi <murtazazaidi@outlook.com>",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "chai": "^4.1.2",
    "mocha": "^4.0.1",
    "nyc": "^11.2.1"
  }
}
